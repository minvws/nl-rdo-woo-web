# https://taskfile.dev
version: "3.48.0"

tasks:
  php:
    desc: Build the PHP image locally
    cmds:
      - >-
        docker build
        --file ./docker/php/Dockerfile
        --tag ghcr.io/minvws/nl-rdo-woo-web-private/php:dev
        {{.BASE_PATH}}
