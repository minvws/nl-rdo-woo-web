<div class="p-4 border-l-2 border-bhr-maximum-red bg-bhr-pale-pink" role="alert">
    <div class="flex">
        <span class="mr-4"><twig:Admin:Icon name="exclamation" color="fill-bhr-maximum-red" /></span>
        <div class="leading-tight pt-1">
            {% block content %}{% endblock %}
        </div>
    </div>

    {% if block('extra') is not empty %}
    <div class="mt-2">
        {% block extra %}{% endblock %}
    </div>
    {% endif %}
</div>
