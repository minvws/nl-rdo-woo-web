<div class="bhr-alert {{ this.alertType }} {{ class }}">
    <div class="flex">
        <span class="mr-4">
            <twig:Admin:Icon color="{{ this.iconColor }}" name="{{ this.iconName }}" size="32" />
        </span>

        <div class="grow pt-1.5">
            {% block content %}{% endblock %}
        </div>
    </div>

    {% if block('extra') is not empty %}
        <div class="mt-4">
            {% block extra %}{% endblock %}
        </div>
    {% endif %}
</div>
