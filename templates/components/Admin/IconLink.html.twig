<a class="hover-focus:text-bhr-ateneo-blue visited:text-bhr-plum" href="{{ href|default('') }}">
    {% block content %}<twig:Admin:Icon name="{{ icon|default('') }}" />{% endblock %}

    <span class="pl-1 text-bhr-sea-blue underline">{{ text|default('') }}</span>
</a>
