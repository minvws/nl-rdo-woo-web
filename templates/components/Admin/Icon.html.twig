<twig:Icon
    class="bhr-icon {{ class|default('') }}"
    color="{{ color|default('fill-bhr-dim-gray') }}"
    size="{{ size|default(24) }}"
    path="assets/img/admin/icons.svg"
    name="{{ name }}"
/>
