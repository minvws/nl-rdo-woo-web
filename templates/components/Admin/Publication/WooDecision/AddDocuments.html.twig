<twig:Admin:Publication:WooDecision:UploadDocuments
    componentName="WooDecisionAddDocumentsController"
    continueLaterUrl="{{ continueLaterUrl }}"
    dossier="{{ dossier }}"
    nextStepUrl="{{ nextStepUrl }}"
    uploadGroupId="{{ uploadGroupId }}"
/>
