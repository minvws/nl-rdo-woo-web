<h3 class="text-xl flex flex-col-reverse">
    <a class="reverse-underline" href="{{ path('app_dossier_detail', { dossierId: entry.dossier.dossierNr }) }}">{{ entry.dossier.title }}</a>

    <ul class="list-unstyled mb-0 items-center pipe-after">
        <li>
            <twig:Icon name="map" class="mr-2" />
            <span class="de-emphasized ro-font-bold">{{ "Woo-dossier" | trans() }}</span>
        </li>
        <li>
            <span class="de-emphasized">{{ "# documents" | trans({"count": entry.dossier.documents|length})}}</span>
        </li>
        <li>
            <span class="de-emphasized">{{ entry.dossier.decision | decision }}</span>
        </li>
    </ul>
</h3>

<p class="ro-text-small leading-5">{{ entry.dossier.summary | u.truncate(200, '...', false) }}</p>

{% if entry.highlights | length > 0 %}
<p class="ro-text-small leading-5 mt-1 mb-2 line-clamp-3">
    {% for hl in entry.highlights %}
        ... {{ hl | highlights }} {% if loop.last %}...{% endif %}
    {% endfor %}
</p>
{% endif %}

<ul class="list-unstyled mb-0 items-center pipe-after">
    <li>
       <span class="de-emphasized">
            <time datetime="{{ entry.dossier.decisionDate | date("Y-m-d") }}">{{ "Decided on" | trans }} {{ entry.dossier.decisionDate | format_date('long') }}</time>,
            <time datetime="{{ entry.dossier.publicationDate | date("Y-m-d") }}">{{ "published on" | trans }} {{ entry.dossier.publicationDate | format_date('long') }}</time>
       </span>
    </li>
    <li>
        <span class="de-emphasized">{{ entry.dossier.dossierNr}}</span>
    </li>
</ul>

