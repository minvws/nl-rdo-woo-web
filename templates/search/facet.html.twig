{% import "facet_macros.html.twig" as macro %}

<h2 class="woo-h2 woo-h2-spacing">Filters</h2>

<form class="form-filters" method="post" action="">
    <fieldset>
        <legend class="woo-legend">{{ "Date" | trans }}</legend>

        <div class="grid gap-4 xs:grid-cols-2 md:grid-cols-1">
            <div>
                <label class="block text-base mb-0.5" for="date-from">{{ "from" | trans }}</label>
                <input class="woo-input-text js-date-filter" id="date-from" name="dt[from]" type="date" value="{{ app.request.get('dt').from | default }}">
            </div>

            <div>
                <label class="block text-base mb-0.5" for="date-to">{{ "to" | trans }}</label>
                <input class="woo-input-text js-date-filter" id="date-to" name="dt[to]" type="date" value="{{ app.request.get('dt').to | default }}">
            </div>
        </div>
    </fieldset>

{#    {{ macro.facet_select('Department' | trans(), constant('App\\Service\\Search\\Model\\FacetKey::DEPARTMENT').value, result) }}#}
    {{ macro.facet_select('Subject' | trans(), constant('App\\Service\\Search\\Model\\FacetKey::SUBJECT').value, result) }}
    {{ macro.facet_select('Source document type' | trans(), constant('App\\Service\\Search\\Model\\FacetKey::SOURCE').value, result) }}
    {{ macro.facet_select('Judgement' | trans(), constant('App\\Service\\Search\\Model\\FacetKey::JUDGEMENT').value, result) }}
    {{ macro.facet_select('Grounds' | trans(), constant('App\\Service\\Search\\Model\\FacetKey::GROUNDS').value, result) }}

    <input type="hidden" name="q" value="{{ app.request.get('q') }}"/>

    <div class="pt-8 js:hidden">
        <button class="woo-button woo-button--primary" type="submit">{{ "Filter results" | trans() }}</button>
    </div>
</form>
