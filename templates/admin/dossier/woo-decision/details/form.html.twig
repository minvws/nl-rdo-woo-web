{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.title) }}
    {{ form_row(form.date_from) }}
    {{ form_row(form.departments) }}
    {{ form_row(form.publication_reason) }}
    {{ form_row(form.default_subjects) }}

    {% if form.children.dossier_nr is defined %}
        {{ form_row(form.dossier_nr) }}
    {% elseif dossier.dossierNr %}
        <div class="bhr-form-row">
            {% if form.children.dossierNr is not defined %}
            <h2 class="bhr-label">{{ 'Reference number'|trans }}</h2>
            {% endif %}
            {{ dossier.dossierNr }}
        </div>
    {% endif %}

    {% if form.documentPrefix is defined %}
        {{ form_row(form.documentPrefix) }}
    {% else %}
        <div class="bhr-form-row">
            <h2 class="bhr-label">{{ 'Prefix for documents'|trans }}</h2>
            <p>{{ "You can no longer edit this after creating the decision file." | trans | raw }}</p>
            <p class="mt-2"><strong>{{ dossier.documentPrefix }}-</strong></p>
        </div>
    {% endif %}
{{ form_end(form) }}
