{% extends 'admin.html.twig' %}

{% block inline_style %}
    {% include 'admin/dossier/concept/workflow-styles.html.twig' %}
{% endblock %}

{% block body %}
    <div class="bhr-container bhr-container--sm">
        <h1 class="bhr-title text-center mb-10">
            {% if workflowStatus.isReadyForDecision %}
                {{ dossier.title }}
            {% else %}
                {{ "New decision file - basic data" | trans() }}
            {% endif %}
        </h1>

        {% include 'admin/dossier/concept/workflow.html.twig' %}

        <div class="bhr-card">
            {% include 'admin/dossier/form/details.html.twig' %}
        </div>
    </div>
{% endblock %}
