{{ form_start(form) }}
    {{ form_row(form.summary) }}

    <twig:Admin:Publication:ManageDocument
        allowedExtensions="{{ documentExtensions }}"
        allowedMimeTypes="{{ documentMimeTypes }}"
        canDelete="{{ canDeleteDocument }}"
        documentType="{{ 'admin.dossiers.investigation-report.document' | trans }}"
        documentTypes="{{ documentTypes }}"
        endpoint="{{ '/balie/api/dossiers/' ~ dossier.id ~ '/investigation-report-document' }}"
        formField="{{ form.document }}"
        grounds="{{ grounds }}"
        helpText="{{ 'admin.dossiers.investigation-report.form.content.document_help' | trans }}"
        languages="{{ attachmentLanguages }}"
        title="{{ 'admin.dossiers.investigation-report.form.content.document_label' | trans }}"
        uploadGroupId="investigation-report-documents"
    />

    <twig:Admin:Publication:ManageAttachments
        allowedExtensions="{{ attachmentExtensions }}"
        allowedMimeTypes="{{ attachmentMimeTypes }}"
        canDelete="{{ canDeleteAttachments }}"
        documentTypes="{{ attachmentTypes }}"
        endpoint="{{ '/balie/api/dossiers/' ~ dossier.id ~ '/investigation-report-attachments' }}"
        grounds="{{ grounds }}"
        helpText="{{ 'admin.dossiers.investigation-report.form.content.attachments_help' | trans }}"
        languages="{{ attachmentLanguages }}"
        uploadGroupId="investigation-report-attachments"
    />

{{ form_end(form) }}
