{% extends 'admin.html.twig' %}

{% set page_title = 'admin.inquiries.manage' | trans %}

{% block body %}
<div class="bhr-container">
    {{ wo_render_breadcrumbs({viewTemplate: 'navigation/breadcrumbs.admin.html.twig' }) }}

    <h1 class="bhr-title pb-6">{{ page_title }}</h1>

    <div class="bhr-card">
        {% set alerts = app.flashes('backend') %}
        {% if alerts | length > 0 %}
            <div class="mb-8">
                {% for messages in alerts %}
                    {% for label, message in messages %}
                        <twig:Admin:Alert type="{{ label }}">
                            <strong>{{ message }}</strong>
                        </twig:Admin:Alert>
                    {% endfor %}
                {% endfor %}
            </div>
        {% endif %}

        {% if inquiries | length > 0 %}
            <table class="bhr-table">
                <thead>
                    <tr>
                        <th class="bhr-column-head bhr-column-head--gray" scope="col">Case nr</th>
                        <th class="bhr-column-head bhr-column-head--gray" scope="col">Database ID</th>
                    </tr>
                </thead>
                <tbody>
                    {% for inquiry in inquiries %}
                        <tr>
                            <td>
                                <a class="bhr-a js-bhr-clickable-row-link" href="{{ path('app_admin_inquiry_administration_details', {casenr: inquiry.casenr}) }}">
                                    {{ inquiry.casenr }}
                                </a>
                            </td>
                            <td>
                                {{ inquiry.id }}
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% else %}
            <p class="text-center">
                Er zijn nog geen zaken aangemaakt.
            </p>
        {% endif %}
    </div>
</div>
{% endblock %}
