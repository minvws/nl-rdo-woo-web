{% extends 'base.html.twig' %}

{% set page_title = "Create new token" | trans() %}

{% block body %}

    <section>
        <div>
            <h1>{{ "Create new token" | trans() }}</h1>

            {{ form(form) }}
        </div>
    </section>

{% endblock %}
