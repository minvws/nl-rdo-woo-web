{% extends 'base.html.twig' %}

{% set page_title = "Create new prefix" | trans() %}

{% block body %}

    <section>
        <div>
            <h1>{{ "Create new prefix" | trans() }}</h1>

            {{ form(form) }}
        </div>
    </section>

{% endblock %}
