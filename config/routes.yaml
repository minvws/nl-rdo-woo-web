controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

app_contact:
    path:          /contact
    controller:    App\Controller\Public\ContentPageController::page
    defaults:
        slug: 'contact'
    options:
        sitemap:
          priority: 0.7
          changefreq: monthly

app_privacy:
    path:          /privacy
    controller:    App\Controller\Public\ContentPageController::page
    defaults:
        slug: 'privacy'
    options:
        sitemap:
          priority: 0.7
          changefreq: monthly
          section: legal

app_about:
    path:          /over-dit-platform
    controller:    App\Controller\Public\ContentPageController::page
    defaults:
        slug: 'over-dit-platform'
    options:
        sitemap:
          priority: 0.7
          changefreq: monthly
          section: legal

app_copyright:
    path:          /copyright
    controller:    App\Controller\Public\ContentPageController::page
    defaults:
        slug: 'copyright'
    options:
        sitemap:
          priority: 0.7
          changefreq: monthly
          section: legal

app_cookies:
    path:          /cookies
    controller:    App\Controller\Public\ContentPageController::page
    defaults:
        slug: 'cookies'
    options:
        sitemap:
            priority: 0.7
            changefreq: monthly
            section: legal

app_accessibility:
    path:          /toegankelijkheid
    controller:    App\Controller\Public\ContentPageController::page
    defaults:
        slug: 'toegankelijkheid'
    options:
        sitemap:
            priority: 0.7
            changefreq: monthly
            section: legal

app_report_vulnerability:
    path:          /kwetsbaarheid-melden
    controller:    App\Controller\Public\ContentPageController::page
    defaults:
        slug: 'kwetsbaarheid-melden'
    options:
        sitemap:
            priority: 0.7
            changefreq: monthly
            section: legal

app_admin_accessibility:
    path:          /balie/toegankelijkheid
    controller:    App\Controller\Admin\IndexController::contentPage
    defaults:
        slug: 'toegankelijkheid'

app_admin_privacy:
    path:          /balie/privacy
    controller:    App\Controller\Admin\IndexController::contentPage
    defaults:
        slug: 'privacy'

app_admin_contact:
    path:          /balie/contact
    controller:    App\Controller\Admin\IndexController::contentPage
    defaults:
        slug: 'balie-contact'
